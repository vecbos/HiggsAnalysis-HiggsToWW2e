# module for the MC truth match for reco muons
# Emanuele Di Marco    29/Apr/2007

# this creates the 1to1 map between reco candidates and gen particles
module muonMatchMap = MCTruthDeltaRMatcher {    
    InputTag src = ambSolving
    InputTag matched = genParticle
    double distMin = 0.1
    vint32 matchPDGId = { 13 }  
}
